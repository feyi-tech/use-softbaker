export interface Theme {
    overlayBg: string,
    widgetBg: string,
    widgetShadow: string,
    widgetColor: string,
    selectButton: {
        bg: string,
        selectedBg: string,
        color: string,
        iconColor: string
    },
}